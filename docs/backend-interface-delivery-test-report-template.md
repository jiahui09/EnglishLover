# EnglishLover 后端接口交付测试报告模板

> 状态：模板保留用于后续接口版本变更。当前正式交付报告见 `docs/backend-interface-delivery-test-report.md`；现行契约版本为 `1.0.0`，已冻结并通过 100% 契约测试。

## 1. 接口清单

| 模块 | 方法 | 路径 | operationId | 状态 | 备注 |
|---|---|---|---|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 变更版本需逐项填写 |

## 2. 契约测试结果

| 契约版本 | 测试命令 | 通过率 | 报告地址 | 执行时间 |
|---|---|---:|---|---|
| 待填写 | `cd backend && npm run verify` | 待填写 | 待填写 | 待填写 |

## 3. 错误码清单

| 错误码 | HTTP 状态 | 语义 | 前端展示建议 |
|---|---:|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 |

## 4. 枚举字段说明

| 枚举名称 | 取值 | 含义 | 变更风险 |
|---|---|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 |

## 5. 测试环境地址

- Base URL：待填写；
- 认证方式：待填写；
- 测试账号/凭据交付方式：通过安全渠道提供，禁止写入仓库。

## 6. `src/types/api.ts` 生成方式与版本号

- 契约来源：`backend/openapi/openapi.yaml`；
- 生成命令：`cd backend && API_CONTRACT_FROZEN=true npm run generate:api-types`；
- 生成工具版本：以后端 `package-lock.json` 为准；
- 生成产物版本：随 OpenAPI `info.version` 记录；
- 冻结评审记录：待对应版本交付时补充。

## 7. 已知限制与后续变更流程

- 字段、枚举、错误码或响应结构冻结后不得直接修改；
- 任何变更必须提交契约变更说明、更新契约测试并重新生成 `src/types/api.ts`；
- 前端页面生成和真实接口接入必须以冻结后的生成类型为唯一来源。
